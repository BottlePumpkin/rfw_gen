name: rfw_gen
description: Convert Flutter Widget code to RFW (Remote Flutter Widgets) format.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  analyzer: ^9.0.0
  rfw: ^1.0.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
