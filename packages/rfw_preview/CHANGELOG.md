## 0.5.2

- No changes (version bump to match rfw_gen_builder)

## 0.5.1

- Update homepage to GitHub Pages playground

## 0.5.0

- Fix: README Quick Start examples now include required `widget` and `library` parameters (#49)
- Fix: replace non-existent `RfwSource.file()` with `RfwSource.asset()` in README (#48)
- Add unit tests for core preview logic (#47)

## 0.4.1

- No changes (version bump to match rfw_gen_builder)

## 0.4.0

- Initial release (version aligned with rfw_gen ecosystem)
- `RfwPreview` widget with `RfwSource` (text, file, asset) support
- `RfwEditorApp` standalone editor with live preview and device frames
- `RfwEditor` embeddable editor widget
- `RfwEditorController` for editor state management
- Snippet storage via `shared_preferences`
